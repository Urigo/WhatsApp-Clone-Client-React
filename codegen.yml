overwrite: true
schema: http://localhost:4000/graphql
documents:
  - ./src/**/*.ts
  - ./src/**/*.tsx
generates:
  ./src/graphql/types.ts:
    plugins:
      - typescript-common
      - typescript-client
