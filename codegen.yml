schema: ../WhatsApp-Clone-server/src/schema.ts
documents: ./src/graphql-documents/**/*.ts
overwrite: true
require:
  - ts-node/register/transpile-only
generates:
  ./src/types/graphql.ts:
    plugins:
      - typescript-common
      - typescript-client
